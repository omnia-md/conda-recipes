package:
  name: openmm
  version: 6.2.rc1

source:
  #url: https://github.com/pandegroup/openmm/archive/master.tar.gz
  url: https://github.com/pandegroup/openmm/archive/6.2rc1.tar.gz
  fn: master.tar.gz

  patches:
    - plugin-dir.patch

build:
  number: 0

requirements:
  build:
    - cmake
    - python
    - fftw3f
    - sphinx
    - sphinxcontrib-bibtex
    # - swig # leave out unless we use VM with workaround because conda swig is broken

  run:
    - python
    - fftw3f

about:
  home: http://openmm.org
  license: MIT
